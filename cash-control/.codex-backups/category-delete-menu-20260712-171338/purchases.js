import * as api from './api.js?v=23';
import { formatBD, showToast, showLoading, navigate, backHeader, screenLayout } from './app.js?v=23';

const esc = (value) => String(value ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const units = ['', 'piece', 'pack', 'box', 'bag', 'kilogram', 'litre', 'bottle', 'carton'];
let data = { categories: [], products: [], suppliers: [], receipt_upload_enabled: false };
let items = [];
let editing = null;
let purchaseFilters = { status: 'active' };
let selectedCategoryId = '';

export function renderPurchaseScreen(screen) {
  if (screen === 'new-purchase' || screen === 'edit-purchase') return '<div class="screen"><div class="spinner center"></div></div>';
  if (screen === 'owner-purchases') return '<div class="screen"><div class="spinner center"></div></div>';
  if (['manage-products','manage-purchase-categories','manage-suppliers'].includes(screen)) return '<div class="screen"><div class="spinner center"></div></div>';
  return '<div class="screen"><p>Screen not found</p></div>';
}

function options(records, selected, empty) { return `<option value="">${empty}</option>${records.map(r => `<option value="${r.id}" ${String(selected)===String(r.id)?'selected':''}>${esc(r.name)}${r.status==='hidden'?' (Hidden)':''}</option>`).join('')}`; }
function total() { return items.reduce((sum, item) => sum + Number(item.line_total || 0), 0); }

function purchaseForm() {
  const p = editing?.purchase || {};
  const mode = p.entry_mode || 'detailed';
  return screenLayout(`${backHeader(editing ? 'Edit Owner Purchase' : 'New Purchase')}
    <form id="purchase-form" class="purchase-form">
      <div class="mode-tabs"><button type="button" class="mode-tab ${mode==='detailed'?'active':''}" data-mode="detailed">Detailed Purchase</button><button type="button" class="mode-tab ${mode==='quick'?'active':''}" data-mode="quick">Quick Purchase</button></div>
      <input type="hidden" id="purchase-mode" value="${mode}">
      <div class="purchase-grid"><label>Purchase Date<input type="date" id="purchase-date" required value="${esc(p.purchase_date || new Date().toISOString().slice(0,10))}"></label><label>Supplier<select id="purchase-supplier">${options(data.suppliers,p.supplier_id,'No supplier')}</select></label></div>
      <section id="detailed-fields" class="${mode==='quick'?'hidden':''}">
        <div class="purchase-step-title"><strong>1. Choose a category</strong><small>Tap a category to show its products</small></div>
        <div id="category-menu" class="category-menu"></div>
        <div class="purchase-step-title"><strong>2. Add products</strong><small>Favourite and recently used products appear first</small></div>
        <input type="search" id="product-search" class="product-search" placeholder="Search products in this menu">
        <div id="product-tiles" class="product-tiles"></div>
        <div class="purchase-step-title"><strong>3. Enter quantity and price</strong><small>Example: 2 cartons × BD 20.000 = BD 40.000</small></div>
        <div id="purchase-items"></div>
      </section>
      <section id="quick-fields" class="${mode==='detailed'?'hidden':''}">
        <label>Total Amount (BD)<input type="number" id="quick-total" min="0.001" step="0.001" value="${mode==='quick'?esc(p.total_amount):''}"></label>
        <label>Category / Description<select id="quick-category">${options(data.categories,p.category_id,'Select category')}</select></label>
        <label>Or custom description<input type="text" id="quick-description" maxlength="120" value="${mode==='quick'&&!p.category_id?esc(p.category_name_snapshot):''}"></label>
      </section>
      <div class="purchase-total"><span>Running Total</span><strong id="purchase-total">${formatBD(mode==='quick'?p.total_amount:total())}</strong></div>
      <label>Receipt (optional) <input type="file" id="purchase-receipt" accept="image/jpeg,image/png,image/webp" ${data.receipt_upload_enabled?'':'disabled'}></label>
      ${data.receipt_upload_enabled?'':'<p class="form-hint">Receipt upload is unavailable until the optional R2 binding is configured.</p>'}
      <label>Note (optional)<textarea id="purchase-note" maxlength="1000">${esc(p.note)}</textarea></label>
      ${editing?'<label>Edit reason *<textarea id="purchase-edit-reason" required></textarea></label>':''}
      <button id="save-purchase" class="btn btn-primary btn-full btn-lg" type="submit">${editing?'Save Purchase Changes':'Save Purchase'}</button>
    </form>`);
}

function renderTiles() {
  const q=(document.getElementById('product-search')?.value||'').toLowerCase();
  const products=data.products.filter(p=>(!q||p.name.toLowerCase().includes(q))&&(!selectedCategoryId||String(p.category_id)===selectedCategoryId));
  document.getElementById('product-tiles').innerHTML=products.map(p=>`<button type="button" class="product-tile" data-product="${p.id}">${p.is_favourite?'★ ':''}${esc(p.name)}<small>${esc(p.category_name||'Uncategorised')}</small></button>`).join('')||'<p class="empty-msg">No products in this category yet. Add products from Settings.</p>';
}

function renderCategoryMenu() {
  const menu=document.getElementById('category-menu'); if(!menu)return;
  const countFor=id=>data.products.filter(p=>String(p.category_id||'')===String(id)).length;
  menu.innerHTML=`<button type="button" class="category-tile ${selectedCategoryId?'':'active'}" data-category=""><strong>All</strong><small>${data.products.length} products</small></button>`+data.categories.filter(c=>c.status!=='hidden').map(c=>`<button type="button" class="category-tile ${String(c.id)===selectedCategoryId?'active':''}" data-category="${c.id}"><strong>${esc(c.name)}</strong><small>${countFor(c.id)} products</small></button>`).join('');
}

function renderItems() {
  const box=document.getElementById('purchase-items'); if(!box)return;
  box.innerHTML=items.map((item,index)=>`<div class="purchase-item" data-index="${index}"><div><strong>${esc(item.product_name)}</strong><button type="button" class="item-remove" data-remove="${index}" aria-label="Remove">×</button></div><div class="item-fields"><label>Quantity<input type="number" min="0.001" step="0.001" data-field="quantity" value="${esc(item.quantity)}" placeholder="e.g. 2"></label><label>Unit<select data-field="unit">${units.map(u=>`<option ${u===item.unit?'selected':''}>${u}</option>`).join('')}</select></label><label>Price each (BD)<input type="number" min="0.001" step="0.001" data-field="unit_price" value="${esc(item.unit_price)}" placeholder="e.g. 20.000"></label><label>Total for product (BD)<input required type="number" min="0.001" step="0.001" data-field="line_total" value="${esc(item.line_total)}" placeholder="0.000"></label><label class="item-note">Item note<input type="text" maxlength="500" data-field="note" value="${esc(item.note)}"></label></div></div>`).join('')||'<p class="empty-msg">Tap a product above to add it.</p>';
  document.getElementById('purchase-total').textContent=formatBD(total());
}

async function loadForm(id) {
  showLoading(true);
  try { data=await api.getPurchaseData(true); editing=id?await api.getOwnerPurchase(id):null; items=(editing?.items||[]).map(i=>({product_id:i.product_id,product_name:i.product_name_snapshot,quantity:i.quantity||'',unit:i.unit||'',unit_price:i.unit_price||'',line_total:i.line_total,note:i.note||''})); selectedCategoryId=''; document.getElementById('app').innerHTML=purchaseForm(); bindForm(); renderCategoryMenu(); renderTiles(); renderItems(); }
  catch(e){showToast(e.message,'error');navigate('owner-dashboard');} finally{showLoading(false);}
}

function bindForm() {
  document.querySelector('[data-action="back"]')?.addEventListener('click',()=>navigate('owner-dashboard'));
  document.querySelectorAll('[data-mode]').forEach(btn=>btn.addEventListener('click',()=>{document.getElementById('purchase-mode').value=btn.dataset.mode;document.querySelectorAll('.mode-tab').forEach(b=>b.classList.toggle('active',b===btn));document.getElementById('detailed-fields').classList.toggle('hidden',btn.dataset.mode==='quick');document.getElementById('quick-fields').classList.toggle('hidden',btn.dataset.mode==='detailed');}));
  document.getElementById('product-search')?.addEventListener('input',renderTiles);
  document.getElementById('category-menu')?.addEventListener('click',e=>{const button=e.target.closest('[data-category]');if(!button)return;selectedCategoryId=button.dataset.category;renderCategoryMenu();renderTiles();});
  document.getElementById('product-tiles')?.addEventListener('click',e=>{const id=e.target.closest('[data-product]')?.dataset.product;if(!id)return;const p=data.products.find(x=>String(x.id)===id);if(items.some(item=>String(item.product_id)===id)){showToast(`${p.name} is already added`,'info');return;}items.push({product_id:p.id,product_name:p.name,quantity:'',unit:p.default_unit||'',unit_price:'',line_total:'',note:''});renderItems();document.querySelector(`[data-index="${items.length-1}"]`)?.scrollIntoView({behavior:'smooth',block:'center'});});
  document.getElementById('purchase-items')?.addEventListener('input',e=>{const row=e.target.closest('[data-index]');if(!row)return;const item=items[Number(row.dataset.index)];const field=e.target.dataset.field;if(!field)return;item[field]=e.target.value;if((field==='quantity'||field==='unit_price')&&item.quantity&&item.unit_price){item.line_total=(Number(item.quantity)*Number(item.unit_price)).toFixed(3);const lineInput=row.querySelector('[data-field="line_total"]');if(lineInput)lineInput.value=item.line_total;}document.getElementById('purchase-total').textContent=formatBD(total());});
  document.getElementById('purchase-items')?.addEventListener('click',e=>{if(e.target.dataset.remove==null)return;items.splice(Number(e.target.dataset.remove),1);renderItems();});
  document.getElementById('quick-total')?.addEventListener('input',e=>document.getElementById('purchase-total').textContent=formatBD(e.target.value));
  document.getElementById('purchase-form').addEventListener('submit',savePurchase);
}

async function savePurchase(e) {
  e.preventDefault(); const button=document.getElementById('save-purchase'); if(button.disabled)return; button.disabled=true;
  const mode=document.getElementById('purchase-mode').value; const payload={entry_mode:mode,purchase_date:document.getElementById('purchase-date').value,supplier_id:Number(document.getElementById('purchase-supplier').value)||null,note:document.getElementById('purchase-note').value};
  if(mode==='detailed'){if(!items.length){showToast('Add at least one product','error');button.disabled=false;return;}payload.items=items;payload.total_amount=total();}else{payload.total_amount=Number(document.getElementById('quick-total').value);payload.category_id=Number(document.getElementById('quick-category').value)||null;payload.category_description=document.getElementById('quick-description').value;}
  if(editing)payload.edit_reason=document.getElementById('purchase-edit-reason').value;
  try{showLoading(true);const result=editing?await api.updateOwnerPurchase(editing.purchase.id,payload):await api.createOwnerPurchase(payload);const id=editing?editing.purchase.id:result.id;const file=document.getElementById('purchase-receipt').files[0];if(file){try{await api.uploadPurchaseReceipt(id,file);}catch(err){showToast(`Purchase saved, but receipt upload failed: ${err.message}`,'error');navigate('owner-purchases');return;}}showToast('Owner purchase saved','success');navigate('owner-purchases');}catch(err){showToast(err.message,'error');button.disabled=false;}finally{showLoading(false);}
}

function purchaseList(result) {
  const f=purchaseFilters;
  return screenLayout(`${backHeader('Owner Purchases')}<form id="purchase-filters" class="filter-panel"><input type="search" id="pq" placeholder="Search supplier, product, category or note" value="${esc(f.q||'')}"><div class="filter-grid"><input type="date" id="pfrom" value="${esc(f.date_from||'')}"><input type="date" id="pto" value="${esc(f.date_to||'')}"><select id="psupplier">${options(data.suppliers,f.supplier_id,'All suppliers')}</select><select id="pcategory">${options(data.categories,f.category_id,'All categories')}</select><select id="pmode"><option value="">All modes</option><option value="detailed" ${f.entry_mode==='detailed'?'selected':''}>Detailed</option><option value="quick" ${f.entry_mode==='quick'?'selected':''}>Quick</option></select><select id="pstatus"><option value="active" ${f.status==='active'?'selected':''}>Active</option><option value="voided" ${f.status==='voided'?'selected':''}>Voided</option><option value="deleted" ${f.status==='deleted'?'selected':''}>Deleted</option><option value="all" ${!f.status?'selected':''}>All statuses</option></select></div><label class="filter-check"><input type="checkbox" id="ptest" ${f.test_only==='1'?'checked':''}> Test only</label><button class="btn btn-primary" type="submit">Apply</button></form><div class="summary-strip"><div><span>Purchases</span><strong>${result.summary.count||0}</strong></div><div><span>Total</span><strong>${formatBD(result.summary.total)}</strong></div></div><div class="tx-list">${result.purchases.map(p=>`<article class="tx-row ${p.is_test?'test':''}"><div class="tx-main"><span class="tx-type">${p.entry_mode==='quick'?'QUICK · ':''}${esc(p.supplier_name_snapshot||p.category_name_snapshot||'Owner Purchase')}</span><span class="tx-amount negative">${formatBD(p.total_amount)}</span></div><div class="tx-meta">${esc(p.purchase_date)} · ${esc(p.category_name_snapshot||'')} · ${esc(p.status)} ${p.is_test?'<span class="badge-test">TEST</span>':''}</div>${p.product_summary?`<div class="tx-note">${esc(p.product_summary)}</div>`:''}<div class="tx-actions"><button class="btn-sm" data-detail="${p.id}">Details</button>${p.receipt_key?`<button class="btn-sm" data-receipt="${p.id}">Receipt</button>`:''}${p.status==='active'?`<button class="btn-sm" data-edit="${p.id}">Edit</button><button class="btn-sm btn-warning" data-void="${p.id}">Void</button><button class="btn-sm btn-danger" data-delete="${p.id}">Delete</button>`:''}</div></article>`).join('')||'<p class="empty-msg">No purchases found.</p>'}</div>`);
}

async function loadPurchases(params=purchaseFilters) {purchaseFilters={...params};showLoading(true);try{const [result,masters]=await Promise.all([api.getOwnerPurchases(purchaseFilters),api.getPurchaseData(true)]);data=masters;document.getElementById('app').innerHTML=purchaseList(result);bindPurchaseList();}catch(e){showToast(e.message,'error');}finally{showLoading(false);} }
function purchaseDetailText(d){const p=d.purchase;const lines=d.items.map(i=>`${i.product_name_snapshot}${i.quantity?` — ${i.quantity} ${i.unit||''}`:''}${i.unit_price?` × ${formatBD(i.unit_price)}`:''}: ${formatBD(i.line_total)}${i.note?`\n  ${i.note}`:''}`);return `${p.entry_mode.toUpperCase()} PURCHASE\nDate: ${p.purchase_date}\nSupplier: ${p.supplier_name_snapshot||'—'}\nCategory: ${p.category_name_snapshot||'—'}\n${lines.join('\n')}\nTotal: ${formatBD(p.total_amount)}\nNote: ${p.note||'—'}\nStatus: ${p.status}`;}
function bindPurchaseList(){document.querySelector('[data-action="back"]')?.addEventListener('click',()=>navigate('owner-dashboard'));document.getElementById('purchase-filters').addEventListener('submit',e=>{e.preventDefault();const status=document.getElementById('pstatus').value;loadPurchases({q:document.getElementById('pq').value,date_from:document.getElementById('pfrom').value,date_to:document.getElementById('pto').value,supplier_id:document.getElementById('psupplier').value,category_id:document.getElementById('pcategory').value,entry_mode:document.getElementById('pmode').value,status:status==='all'?'':status,test_only:document.getElementById('ptest').checked?'1':''});});document.getElementById('app').addEventListener('click',async e=>{const id=e.target.dataset.detail||e.target.dataset.edit||e.target.dataset.void||e.target.dataset.delete||e.target.dataset.receipt;if(!id)return;try{if(e.target.dataset.edit)navigate('edit-purchase',{id});else if(e.target.dataset.receipt)await api.openPurchaseReceipt(id);else if(e.target.dataset.detail){alert(purchaseDetailText(await api.getOwnerPurchase(id)));}else if(e.target.dataset.void){const reason=prompt('Void reason required:');if(reason){await api.voidOwnerPurchase(id,reason);loadPurchases();}}else if(confirm('Delete this purchase?')){await api.deleteOwnerPurchase(id);loadPurchases();}}catch(err){showToast(err.message,'error');}});}

const resources={ 'manage-products':['products','Products'], 'manage-purchase-categories':['purchase-categories','Product Categories'], 'manage-suppliers':['suppliers','Suppliers'] };
function masterNameFields(resource){if(resource!=='products')return '<label>Name<input id="master-name" required maxlength="120"></label>';return `<div class="bulk-product-head"><strong>Product names</strong><small>All products below will use the same category and default unit.</small></div><div id="bulk-product-names" class="bulk-product-names"><div class="bulk-product-row"><input class="bulk-product-name" required maxlength="120" placeholder="Product name"><button type="button" class="btn-sm btn-danger bulk-product-remove" aria-label="Remove product">×</button></div></div><button type="button" id="add-product-row" class="btn btn-secondary btn-full">+ Add another product</button>`;}
function masterExtraFields(resource){if(resource==='products')return `<label class="bulk-more-products">+ Add more products at once <span class="optional">(one product per line)</span><textarea id="master-more-products" rows="5" maxlength="3000" placeholder="Shrimp&#10;Chapati&#10;Ginger strips"></textarea></label><label>Category for all products<select id="master-category">${options(data.categories,'','No category')}</select></label><label>Default unit for all<select id="master-unit">${units.map(u=>`<option>${u}</option>`).join('')}</select></label><label>Starting display order<input id="master-order" type="number" step="1" value="0"></label>`;if(resource==='suppliers')return `<label>Location<input id="master-location" maxlength="200"></label><label>Phone<input id="master-phone" maxlength="50"></label><label>Note<textarea id="master-note" maxlength="500"></textarea></label>`;return `<label>Display order<input id="master-order" type="number" step="1" value="0"></label>`;}
function masterPayload(resource){const payload={name:document.getElementById('master-name').value};if(resource==='products'){payload.names=[payload.name,...document.getElementById('master-more-products').value.split(/\r?\n/)].map(name=>name.trim()).filter(Boolean);payload.category_id=Number(document.getElementById('master-category').value)||null;payload.default_unit=document.getElementById('master-unit').value;payload.display_order=Number(document.getElementById('master-order').value)||0;}else if(resource==='suppliers'){payload.location=document.getElementById('master-location').value;payload.phone=document.getElementById('master-phone').value;payload.note=document.getElementById('master-note').value;}else payload.display_order=Number(document.getElementById('master-order').value)||0;return payload;}
function masterMeta(resource,r){if(resource==='products')return `${r.category_name||'No category'} · ${r.default_unit||'No unit'} · order ${r.display_order}`;if(resource==='suppliers')return [r.location,r.phone,r.note].filter(Boolean).join(' · ')||'No additional details';return `Order ${r.display_order}`;}
async function loadMaster(screen,q=''){const [resource,title]=resources[screen];showLoading(true);try{const [result,masters]=await Promise.all([api.listMasterData(resource,{include_hidden:'1',q}),api.getPurchaseData(true)]);data=masters;document.getElementById('app').innerHTML=screenLayout(`${backHeader(title)}<form id="master-search" class="filter-panel"><input id="master-q" type="search" value="${esc(q)}" placeholder="Search ${title.toLowerCase()}"><button class="btn btn-secondary" type="submit">Search</button></form><div class="form-card"><form id="master-add" class="master-form"><label>Name<input id="master-name" required maxlength="120"></label>${masterExtraFields(resource)}<button class="btn btn-primary btn-full" type="submit">Add ${title.replace(/s$/,'')}</button></form></div><div class="tx-list">${result.records.map(r=>`<div class="tx-row"><div class="tx-main"><strong>${resource==='products'&&r.is_favourite?'★ ':''}${esc(r.name)}</strong><span>${esc(r.status)}</span></div><div class="tx-meta">${esc(masterMeta(resource,r))}</div><div class="tx-actions">${resource==='products'?`<button class="btn-sm" data-favourite="${r.id}" data-value="${r.is_favourite?0:1}">${r.is_favourite?'Unfavourite':'Favourite'}</button>`:''}<button class="btn-sm" data-edit-master="${r.id}">Edit</button><button class="btn-sm" data-order="${r.id}" data-value="${r.display_order||0}">Reorder</button><button class="btn-sm" data-status="${r.id}" data-value="${r.status==='active'?'hidden':'active'}">${r.status==='active'?'Hide':'Reactivate'}</button></div></div>`).join('')||'<p class="empty-msg">No records found.</p>'}</div>`);document.querySelector('[data-action="back"]').addEventListener('click',()=>navigate('settings'));document.getElementById('master-search').addEventListener('submit',e=>{e.preventDefault();loadMaster(screen,document.getElementById('master-q').value)});document.getElementById('master-add').addEventListener('submit',async e=>{e.preventDefault();try{await api.createMasterData(resource,masterPayload(resource));showToast('Saved','success');loadMaster(screen,q);}catch(err){showToast(err.message,'error');}});document.getElementById('app').addEventListener('click',async e=>{const id=e.target.dataset.editMaster||e.target.dataset.status||e.target.dataset.favourite||e.target.dataset.order;if(!id)return;const record=result.records.find(r=>String(r.id)===String(id));let change;if(e.target.dataset.editMaster){const name=prompt('Name:',record.name);if(name===null)return;change={name};if(resource==='products'){const category=prompt('Category ID (blank for none):',record.category_id||'');if(category===null)return;const unit=prompt('Default unit:',record.default_unit||'');if(unit===null)return;change.category_id=category?Number(category):null;change.default_unit=unit;}else if(resource==='suppliers'){const location=prompt('Location:',record.location||'');if(location===null)return;const phone=prompt('Phone:',record.phone||'');if(phone===null)return;const note=prompt('Note:',record.note||'');if(note===null)return;Object.assign(change,{location,phone,note});}}else if(e.target.dataset.order){const order=prompt('Display order:',e.target.dataset.value);if(order===null)return;change={display_order:Number(order)};}else if(e.target.dataset.favourite)change={is_favourite:Number(e.target.dataset.value)};else change={status:e.target.dataset.value};try{await api.updateMasterData(resource,id,change);showToast('Updated','success');loadMaster(screen,q);}catch(err){showToast(err.message,'error');}});}catch(err){showToast(err.message,'error');}finally{showLoading(false);}}

export function purchaseScreens(screen, screenData={}) { if(screen==='new-purchase')loadForm();else if(screen==='edit-purchase')loadForm(screenData.id);else if(screen==='owner-purchases')loadPurchases();else if(resources[screen])loadMaster(screen); }
